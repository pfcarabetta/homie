/**
 * @homie/shared — types and pure utilities shared across web + mobile.
 *
 * Sub-paths:
 *   import { ApiResponse, AccountBooking } from '@homie/shared/types';
 *   import { cleanPrice, timeAgo } from '@homie/shared/utils';
 *
 * Or import everything from the package root:
 *   import { ApiResponse, cleanPrice } from '@homie/shared';
 */
export * from './types';
export * from './utils';
